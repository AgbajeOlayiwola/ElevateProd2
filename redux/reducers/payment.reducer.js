// import AuthTypes from '../types/auth.types';

// const initialState = {
//     createAccount: null
// };

export const paymentReducer = (state = { payment1: 'Tope' }, action) => {
    return state;
};
