import React from 'react';
import Link from 'next/link';
import { CardWrapper } from './styles.module';

const ProfileCard = ({ width, height, text }) => {
    return;
    <CardWrapper style={{ width, height }}>{text}</CardWrapper>;
};

export default ProfileCard;
