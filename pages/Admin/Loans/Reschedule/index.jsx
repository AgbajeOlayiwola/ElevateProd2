import React from 'react';
import RequestCont from '../../../../components/ReusableComponents/RequestCont';

const Reschedule = () => {
    return <RequestCont title="Reschedule payment" type={false} />;
};

export default Reschedule;
