import React from 'react';
import { Navbar } from '../../components';

const About = () => {
    const me = ['ola', 'new'];

    return (
        <div>
            <h1>About</h1>
        </div>
    );
};

export default About;
